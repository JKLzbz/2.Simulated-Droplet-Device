# 💧 研电赛校赛答辩准备指南与 PPT 大纲 (13小时冲刺版)

本指南针对**第二十一届研电赛**（全国研究生电子设计竞赛）校内选拔赛的紧急准备而编写，结合了您项目的核心技术特点与外壳工程设计。

---

## 1. 报名赛道确认与合规性

根据对官方文档 `2026第二十一届研电赛赛题指南及清单.pdf` 第 8 页和第 260 页的检索，您的项目完全符合**“电路与嵌入式系统类”**赛道的要求：

> **（一）电路与嵌入式系统类**
> 包括但不限于针对某一功能应用所开展的具有较强创新创意的电子电路软硬件设计、终端设备或嵌入式系统实现等，如基于 FPGA、DSP、MCU、嵌入式系统等开发的软硬件系统、智能硬件等。

**项目契合点：**
* **控制芯片**：采用 ESP32/STM32 等嵌入式 MCU 作为核心控制硬件。
* **软件算法**：下位机固件采用 C++ 开发，具备非阻塞状态机与多变体控制逻辑。
* **硬件系统**：包含定制 PCB、电磁阀驱动电路、电源管理和一体化干湿分离工程外壳。
* **上位机软件**：Python PyQt 串口交互界面。

---

## 2. 13 小时紧急冲刺策略：如何化解“没有实体外壳”的尴尬

评委老师在校赛阶段主要考核**方案可行性、技术创新点和系统完整性**。面对尚未打印完成的外壳，建议在答辩时采取以下应对策略：

1. **不要隐瞒，主动展示成熟度**：
   - 坦白告知评委：“目前电路与软件控制系统已完全开发完毕并测试通过。结构外壳设计已全部完成，目前正在进行 SLA 工业级光敏树脂 3D 打印生产，将在分区赛前组装完毕。”
2. **展示 3D 概念效果图（PPT 加分利器）**：
   - 在 PPT 的“系统结构设计”或“工程化落地”页面，贴出以下为您生成的**三维外壳渲染概念图**。这能向评委证明，你们并不是“只做面包板接线的半成品”，而是有完整工业设计与产品化考量的成熟作品。

![Enclosure 3D Render](enclosure_3d_render_1780514466976.png)

---

## 3. 研电赛答辩 PPT 结构大纲设计 (建议 6-8 分钟)

由于时间紧张，PPT 无需堆砌文字，以**图表、实物运行照片/视频、逻辑图**为主。

### 第 1 页：封面页 (立意要高)
* **主标题**：基于嵌入式边缘控制的多变体微液滴发生与喷射模拟系统
* **副标题**：一种用于咳嗽流体动力学模拟的工程化解决方案
* **团队信息**：学校名称、团队名称、指导老师、参赛赛道（电路与嵌入式系统类）

### 第 2 页：立项背景与痛点分析 (Judge Care!)
* **背景描述**：液滴喷射模拟在气溶胶传播研究、公共卫生安全、呼吸系统流体模拟中具有重要科学价值。
* **传统痛点**：
  1. 传统模拟设备功能单一，无法还原人咳嗽时“复杂、多阶段、突发性”的喷射曲线。
  2. 控制系统抗噪性差，气路与电路未隔离，实验安全隐患大。
* **研究目标**：开发一套参数化控制、具备多种生理咳嗽变体还原能力的嵌入式精密喷射模拟系统。

### 第 3 页：系统总体架构设计 (软硬件结合)
* **系统框架图**（用简单的框图表达）：
  - **上位机（Host）**：Python PyQt 图形化控制台，实现变体参数化设置、波形配置与串口防干扰协议封装。
  - **下位机（Slave）**：ESP32 边缘控制器，通过 C++ 固件解析串口指令，使用非阻塞硬件时序驱动阀、泵和传感器。
  - **机械结构（Chassis）**：干湿分离一体化物理屏蔽箱。

### 第 4 页：核心技术：非阻塞时序与生理咳嗽变体模拟 (技术硬核点)
* **技术创新 1**：基于状态机和定时器中断的非阻塞硬件时序控制，时间精度达到微秒级，完美避免流体控制中的任务阻塞。
* **变体库展示**（用曲线图或表格展示）：
  - `Original`（原始爆发模式）
  - `Quick Light`（轻度快速咳嗽）
  - `Heavy Deep`（深度重度爆发）
  - `Double Burst`（双重连续爆破）
* **效果**：精确控制气罐充气气压与电磁阀喷射时值，输出与真实医学咳嗽特征高度一致的液滴曲线。

### 第 5 页：系统集成与工程化设计 (物理封装与安全性)
* **干湿分离物理隔离设计**：
  - **左侧（湿区/气路舱）**：气泵、蓄压气罐、电磁阀和喷射口。
  - **右侧（干区/电路舱）**：ESP32 核心板、驱动板、高效率 170x110mm 车载级电源，彻底杜绝液体泄漏导致的电气短路风险。
* **过线与散热通道**：共轴接线孔与防水隔板多路对齐布线（对准主板与隔板过线孔）。
* **物理展示**：贴上外壳 3D 渲染图（即上图），并展示当前的“裸板功能调试照片/短视频”，证明功能已跑通。

### 第 6 页：实验结果与展望
* **数据对比**：上位机发送指令与下位机实测电磁阀开关时延曲线，证明通信与执行的高精度。
* **下一步计划**：
  1. 3D 打印外壳到货后进行整机封装。
  2. 定制 $580\times 580\text{ mm}$ 的高透防爆亚克力顶盖平铺，进行整体风道与防水压力测试。
  3. 参加后续的分区赛展示。
* **结语**：感谢各位评委老师，请多多指教！

---

## 4. 校赛答辩当场演示 (Demo) 避坑指南

1. **录制“备用视频”**：
   - 现场答辩时间短、串口线容易接触不良。**千万不要只指望现场演示！**
   - 马上用手机录制一段 1 分钟左右的“裸板功能运行视频”：展示在 Python 上位机点击不同咳嗽模式，下位机指示灯对应闪烁、泵或阀按特定节拍动作的画面。把视频嵌入 PPT 里，现场即使演示失败也可以直接播放视频。
2. **重点突出功能跑通**：
   - 评委问及外壳时，重点强调“结构图纸已全尺寸出图并送交 3D 打印厂加工，软硬件已经处于随时可组装状态”。

祝您校赛答辩顺利，拿到分区赛名额！

---

## 5. 检测端 (STM32F407) 接收与处理端核心算法 (### 407接收处)

在答辩 PPT 的“测试数据处理”或“边缘计算”页中，可直接使用以下学术整理：

### 407 接收与处理端核心算法

#### (1) 边缘端多源数据时间对齐与报文融合
* **采样周期**：以 $10\text{ ms}$（TIM2 定时器中断）为最小物理时间戳窗口。
* **数据融合**：将电容检测（液滴体积特征 $C$）、激光测距（飞行距离 $D$）、冲击加速度与角速度（运动特征）、环境温度等**时空异构数据**在边缘端做高精度对齐，并通过自定义的 29 字节紧凑报文发送。

#### (2) 运动特征的边缘降维与矢量合成 (L2 范数算法)
为了降低无线传输占用的网络开销，避免 MPU6050 原始 6 轴数据占用过多网络传输周期导致延迟，STM32F407 在边缘端直接进行了**矢量的 L2 范数合成（数据降维）**：
* **合成加速度 ($a_{\text{sum}}$)**：
  $$a_{\text{sum}} = \sqrt{a_x^2 + a_y^2 + a_z^2}$$
* **合成角速度 ($\omega_{\text{sum}}$)**：
  $$\omega_{\text{sum}} = \sqrt{g_x^2 + g_y^2 + g_z^2}$$
* **学术/答辩包装（关于加速度计的保留意义）**：
  * **物理层面的合理性**：即使装置主体是静态的，液滴喷射时电磁阀的瞬间敲击、气泵动作以及流体爆发的反作用力，都会引起安装支架的**瞬态冲击振动**。
  * **学术价值**：MPU6050 在此用于**“喷射反作用力冲击监测（Thrust/Vibration Detection）”**与**“水平姿态自校准（Levelling Sensor）”**，以保证装置垂直喷射的绝对平行，确保实验结果的高重复性。

#### (3) 电容传感器的物理换算模型 (FDC2214 谐振逆推)
STM32F407 从 FDC2214 读取的 28 位原始频率计数值（Data_FDC）通过基于电容-电感谐振电路物理特性的逆推模型进行实时电容值换算：
* **换算模型**：
  $$C = \left(\frac{K}{\text{Data\_FDC}}\right)^2$$
  *(其中 $K = 232024597.611245$ 为硬件电路谐振常数，在代码中写为 `Cap = 232024597.611245 / Data_FDC`)*

#### (4) 滤波与降噪算法说明 (卡尔曼滤波与 DLPF 机制)
关于卡尔曼滤波与降噪：
* **硬件级数字低通滤波器 (DLPF)**：我们调用的 MPU6050 底层库（`inv_mpu.c`）中，已经在芯片寄存器层面直接使能了**硬件 DLPF（数字低通滤波器，工作在 42Hz/20Hz 频段）**，滤除了绝大部分外界高频震动噪声。
* **物理谐振积分滤波**：FDC2214 芯片本身内部包含高阶积分计算（LC tank 的多采样周期转换设置 `CLOCK_DIVIDERS` 与 `DRIVE_CURRENT`），相当于在硬件级完成了频域积分滤波，保证电容输出的极高信噪比。
* **上位机滑窗平滑滤波器**：上位机对接收到的电容信号进行滑动平均（Moving Average Filter），用于滤除电磁阀启闭时瞬间产生的反向电动势干扰，确保液滴识别的精准性。

