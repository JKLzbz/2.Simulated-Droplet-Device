import os

file_path = 'D:/Second Brain/23Carrer/05.中断.md'

with open(file_path, 'r', encoding='utf-8', errors='ignore') as f:
    content = f.read()

# Find the index of '## 二'
index = content.find('## 二')
if index != -1:
    content = content[:index]

new_part = '''## 二. 407 为什么用 TIM 中断采电容？（快慢分离架构的核心）

在我们的飞沫监测设备中，最核心的数据就是 **100Hz 的电容波形**。为了保证波形的绝对完美，我们使用了 **TIM 中断**。

### 1. 什么是 TIM 中断（定时器中断）？
**比喻**：“极其守时的死亡闹钟”。
我们在单片机内部设置了一个名为 `TIM2` 的独立硬件定时器，它不受 CPU 影响，自己默默数秒。我们在代码里设定它**每 10 毫秒（100Hz）准时炸响一次**！

### 2. 电容采集为什么必须放在 TIM 中断里？
*   飞沫掠过传感器可能只有短短的 20 毫秒。如果把电容采集放在主循环里，当单片机正在费力地连接 WiFi 或处理 UI 时，这 20 毫秒就直接错过了，波形就会掉帧甚至丢失。
*   放在 TIM 中断里意味着：只要 10 毫秒的闹钟一响，**单片机必须立刻强行打断 WiFi 发送等一切工作**，瞬间冲过去把 FDC2214 的电容值读出来，丢进 `FIFO_buffer`（内存队列）里。等读完之后，再赶紧跑回去接着干发 WiFi 的活。
*   **总结**：正是因为电容采集被霸道地放在了 `TIM2_IRQHandler` 定时器中断里，它才能雷打不动地保持 100Hz 的极速完美波形！而 WiFi 打包发送，只能在主循环里委屈巴巴地等着中断执行完，再去默默搬运中断留下的数据。这就是本项目 **“快慢分离”** 架构的最强核心！

---

## 三. 单片机里到底有哪些常见的中断类型？

“中断（Interrupt）”是单片机极其庞大的一个家族。单片机（以 STM32 为例）拥有一个危机响应中心叫 **NVIC（嵌套向量中断控制器）**，它支持几十上百种不同的中断源。
为了方便消化，我们把常见的中断分成以下几种“生活场景”：

### 1. TIM 中断 (定时器中断) —— “守时的闹钟”
*   **场景**：你需要单片机**极其严格、分毫不差**地每隔一段时间执行一个动作。
*   **我们的应用**：`TIM2` 中断，严格保持每 10ms（100Hz）采集一次电容波形。

### 2. USART 中断 (串口接收中断) —— “快递员砸门”
*   **场景**：外部设备（如 ESP8266 WiFi 模块、蓝牙、上位机）随时可能给你发来一行数据。
*   **逻辑**：你不能让单片机死盯着串口看。所以平时单片机干自己的活，只要串口硬件一收到新字节，立马触发 USART 中断，单片机瞬间把数据拿进内存保护起来，防止被下一个到达的数据覆盖。

### 3. EXTI 中断 (外部引脚中断) —— “按门铃”
*   **场景**：专门用来捕获**物理引脚的电平变化**（比如按键按下、传感器报警引脚被拉低）。
*   **逻辑**：比如休眠状态下的智能门锁，CPU 全面停工省电。只要你手一按密码键盘，引脚电平一变，立马触发 EXTI 外部中断，瞬间把 CPU 从休眠中“扇醒”起来干活。

### 4. DMA 中断 (直接内存访问完成中断) —— “外包团队干完活汇报”
*   **场景**：单片机需要搬运海量数据，比如把一张 500KB 的照片存进 SD 卡。
*   **逻辑**：如果让 CPU 自己去搬，它会被活活累死（假死几秒钟）。所以 CPU 会把这活儿外包给 DMA（专门搬砖的独立硬件）。DMA 在后台默默搬了几十万个字节，搬完之后触发 DMA 中断，大喊一声：“老板，活干完了！”，CPU 这才过来检查成果。

### 5. ADC / I2C / SPI 硬件通信中断 —— “体检报告出炉”
*   **场景**：硬件测量需要耗费微秒级别的时间（比如 ADC 转换电池电压）。
*   **逻辑**：启动测量后，CPU 不需要在原地死等。等硬件慢吞吞地测量完了，它会触发相应的中断，通知 CPU 直接过来取走现成的数值。

**核心总结：**
*   **主循环** 是用来干“又脏又累又不着急”的体力活（处理业务逻辑、打包网络）。
*   **中断** 是用来处理“极度对时间敏感、一眨眼就会错过”的突发事件。
掌握了这两者的配合，你就彻底弄懂了单片机系统的灵魂！
'''

with open(file_path, 'w', encoding='utf-8') as f:
    f.write(content + new_part)
